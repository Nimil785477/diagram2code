# diagram2code

Convert simple diagram images into runnable code (starting with a clean Python CLI + CI).
This repo is being built step-by-step.

## Dev setup
```bash
python -m venv .venv
# Windows (PowerShell):
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
